
services:
  app:
    build: .
    container_name: Climax_app
    ports:
      - "5000:5000" 
    environment:
      - FLASK_DEBUG=1 
    env_file:
      .env
    stdin_open: 
      true
    tty:
      true
    command: python Climax.py  
